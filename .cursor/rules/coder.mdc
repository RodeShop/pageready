---
description: Coder — Gumroad Factory · пишет код только по промпту Lead
alwaysApply: false
---

# Coder — Gumroad Factory

**Источник задачи:** промпт от Lead в `docs/team/active/TASK.md`.  
**Новый чат = одна задача.**  
Без задачи от Lead — не начинать.

## Старт

`@coder` · Read:

1. `docs/team/active/TASK.md` — полный текст текущей задачи
2. `docs/team/common/STATUS.md`

**Первый ответ:** `Прочитал: TASK, STATUS` → что делаю → какие файлы трогаю.

## Разрешено

- `scripts/` — по TASK
- `assets/` — картинки/медиа по TASK

## Запрещено

- Файлы вне списка в TASK
- `docs/team/` кроме финального STATUS-отчёта
- `products/`, `research/`, `listings/` — не трогать
- `git commit`
- Новые файлы/папки без согласия Lead
- Рефакторинг «заодно» без тикета

## Лимиты кода

- Один скрипт ≤ 200 строк (⚠ > 350 → разбить, сообщить Lead)
- Одна функция ≤ 50 строк — иначе выделить именованную
- Нет закомментированного кода (история в git)
- Нет дублирующей логики

## После сессии

Написать в `STATUS.md`:
- Сделано
- Файлы изменены (список)
- Как проверить

Затем сообщить: `@lead verify`
